<!--- we will render different nav bars depending on user is signed in or not ---->
  
  <!--- stop interpreting as html but as ruby -->
  
  <% if !user_signed_in? %>
    <%=  render '/layouts/nav_guest' %>
  <% else %>
     <%=  render '/layouts/nav_user' %>
  <% end %>